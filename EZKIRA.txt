EZKIRA


nama database : unztjxybjh_ezkira
username : unztjxybjh_ezkira
password : Ezkira12345!


Dalam cPanel → phpMyAdmin → pilih database unztjxybjh_ezkira → klik tab SQL → run:

SELECT COUNT(*) as total_users, 
       SUM(CASE WHEN role = 'client' THEN 1 ELSE 0 END) as clients,
       SUM(CASE WHEN role = 'admin' THEN 1 ELSE 0 END) as admins
FROM users;
Atau kalau nak tengok senarai semua user:

SELECT id, name, email, role, created_at FROM users ORDER BY created_at DESC;